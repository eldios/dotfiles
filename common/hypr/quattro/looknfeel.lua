-- Personal look and feel overrides.
--
-- Gaps and border width carried over from the pre-Quickshell config. Colors
-- are not set here: the active theme supplies them.

hl.config({
  general = {
    layout = "dwindle",
    resize_on_border = true,
    gaps_in = 5,
    gaps_out = 10,
    border_size = 2,
    allow_tearing = false,
  },
})
