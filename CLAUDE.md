# Repository rules

- Do not create or modify anything under `docs/`, or `README.md`, without
  explicit permission for that specific change. Documentation here is written
  together, after the work has settled, never as a side effect of it.
